# V3 Phase A Complete (2026-04-05)

## Co zrobilem

### Module 2: OnboardingFlow (`agent_core/orchestrator/onboarding.py`)
- 5 krokow: Kim jestem? -> Co potrafie? -> Jak sie ucze? -> Ograniczenia -> Gotowa!
- Detekcja: `should_run()` sprawdza flag `onboarding_completed` w IdentityStore
- Dual output: `run()` dla REPL (tekst), `get_steps()` dla Web UI (dict)
- Persistence: `mark_completed(preferences=)` / `reset()`
- Autonomy presets (low/medium/high) do wyboru przez operatora

### Module 3: UserFacingSelfModel (`agent_core/orchestrator/self_model_facade.py`)
- Agreguje: SelfModelBuilder + CapabilityRouter + ContextBuilder + IdentityStore + HomeostasisCore
- get_status(): full dashboard dump (identity, personality, capabilities, awareness, limitations, mode)
- describe_self(): chatty self-description w PL
- get_capabilities_grouped(): kategorie (Nauka, Samoanaliza, System, Zewnetrzne, Internet, Monitoring)
- get_system_prompt_context(): compact context for LLM injection
- get_limitations(): dynamic list (sprawdza OpenClaw, NIM availability)
- Lazy loading z ctx - dziala nawet z czesciowo zainicjalizowanym systemem

### Wiring (`maria.py`)
- Po `registry.init_all(ctx)` tworzy UserFacingSelfModel + OnboardingFlow
- Pierwsza uruchomienie: wyswietla onboarding automatycznie (pomija w daemon mode)
- SharedContext rozszerzony o 4 nowe pola: capability_router, context_builder, user_facing_self_model, onboarding_flow

### Testy
- 65 nowych testow w test_orchestrator.py
- 3131 total (0 failures)

## Architektura

```
agent_core/orchestrator/
  __init__.py           # Exports: UserFacingSelfModel, OnboardingFlow
  self_model_facade.py  # Module 3: unified self-model API
  onboarding.py         # Module 2: first-run guidance
```

V3 = wrap, adapt, expose, orchestrate (NIE rewrite).
Cala logika biznesowa zostaje w V2 (K1-K13, consciousness, routing).
Orchestrator tylko agreguje i prezentuje.

## Decyzje

1. **UserFacingSelfModel przed OnboardingFlow** - Module 3 jest zaleznoscia Module 2
2. **Lazy loading z ctx** - mozna stworzyc self_model przed pelnla inicjalizacja modulow
3. **OnboardingStep jako slots-based class** - lekki, JSON-serializable
4. **_TAG_GROUPS mapping** - tagi z CapabilitySpec -> polskie nazwy kategorii
5. **Autonomy presets** - 3 poziomy mapowane na authority levels (na przyszlosc)

## V3 Phase B - Task Pipeline (ta sama sesja)

### Module 5: TaskDecomposer (`agent_core/orchestrator/task_decomposer.py`)
- Keyword-based classification (zero LLM): 6 kategorii
- TaskCategory: LEARN_TOPIC, EXPLORE_NEW, CONSOLIDATE, ANALYZE, FETCH_INFO, SYSTEM_CHECK
- Topic extraction (usuwanie stop words PL+EN)
- Step building mirrorujacy K8 templates (fallbacks, retries)
- Feasibility check (mode, network)

### Module 6: ExecutionPlanBuilder (`agent_core/orchestrator/execution_plan.py`)
- StepConstraint: LLM call estimates, K7 classification, availability, blocked status
- ExecutionPlan: is_executable, describe(), to_dict()
- Warnings: REDUCED mode, network needs, blocked steps

### Module 4: TaskOrchestrator (`agent_core/orchestrator/task_orchestrator.py`)
- submit(description) -> decompose + plan + return for review
- approve(task_id) -> creates Goal in GoalStore (ACTIVE, type from category)
- cancel(task_id) -> abandons Goal
- get_progress(task_id) -> syncs from Goal state (achieved->COMPLETED etc)
- list_tasks(status=) -> summary listing
- In-memory TaskRecord store (cap 50, auto-trim terminal)
- Goal metadata: task_id, category, topic, source="v3_orchestrator"

### Testy: 70 nowych (3201 total)

## V3 Phase C - Practical Intelligence (ta sama sesja)

### Module 7: CostEstimator (`agent_core/orchestrator/cost_estimator.py`)
- ActionCost: nim_tokens, local_llm_calls, external_calls, is_free
- PlanCost: aggregated + budget warnings
- BudgetStatus: nim remaining, claude/codex hourly limits
- Dane: _ACTION_COSTS dict (learn=3local, self_analyze=2500nim, etc)

### Module 8: TimeEstimator (`agent_core/orchestrator/time_estimator.py`)
- TimeEstimate: seconds, label, confidence (high/medium/low)
- PlanTimeEstimate: total + cold start penalties (model loading)
- Dane: _ACTION_TIMES dict (learn=180s, evaluate=5s, fetch=15s, etc)
- _format_duration(): "~3 min", "~1 godz"

### Module 9: FreeVsPaidPlanner (`agent_core/orchestrator/free_vs_paid.py`)
- ResourceStrategy: LOCAL_ONLY, PREFER_LOCAL, MIXED, PREFER_PAID, PAID_REQUIRED
- Per-action: learn->ollama, self_analyze->nim, ask_expert->codex cascade
- Per-plan: overall strategy, nim spend estimate, savings_if_local
- Budget-aware: warns when NIM spend > remaining

### Testy: 49 nowych (3250 total)

## Co dalej

V3 Phase D: Execution Bridge
- Module 10: ExecutionRouter (wraps CapabilityRouter)
- Module 11: ToolCapabilityRegistry (user-facing)
- Module 12: TaskProgressTracker
- Module 13: LimitationReporter

## V3 Phase E - Product Hardening (ta sama sesja)

### Module 14: ProductShell (`agent_core/orchestrator/product_shell.py`)
- Unified facade laczacy wszystkie 13 modulow V3
- do(task) -> decompose + plan + cost + time + resources (full enrichment)
- approve/cancel/progress/tasks
- who_am_i/what_can_i_do/limitations (text output)
- get_status() -> full dashboard dict
- do_and_describe() -> human-readable summary PL

### Module 15: V3Module REPL (`agent_core/modules/v3_module.py`)
- /v3 command z 12 subcommandami
- /v3 do <zadanie> - zlecanie zadan
- /v3 approve/cancel/progress <task_id> - lifecycle
- /v3 tasks - lista zadan
- /v3 who/what/limits - identity
- /v3 tools/budget - discovery
- /v3 onboarding - restart onboarding

### Testy: 26 nowych (3317 total)

## V3 COMPLETE
Wszystkie 15 modulow, 5 faz, 251 testow orchestrator.
Teraz potrzebny restart Marii aby wlaczyc:
- python maria.py (zamiast run_maria.py)
- /v3 w REPL
